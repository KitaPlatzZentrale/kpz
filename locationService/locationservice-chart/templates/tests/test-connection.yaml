apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "locationservice-chart.fullname" . }}-test-connection"
  labels:
    {{- include "locationservice-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "locationservice-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
