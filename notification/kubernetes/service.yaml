# api-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: notificationservice-service
spec:
  selector:
    app: notificationservice
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30003 # Choose a NodePort value (e.g., 30000) or let Kubernetes allocate one automatically.
